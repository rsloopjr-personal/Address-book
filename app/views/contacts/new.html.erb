<div class="container">
  <div class="row">
    <div class="col">
    </div>
    <div class="col-5 text-center">
        <h1>New Contact</h1>

        <%= render 'form', contact: @contact %>
        <a class="btn btn-primary btn-sm" href="<%= contacts_path %>" role="button">Back</a>
    </div>
    <div class="col text-right">
      <%= link_to("Sign Out", destroy_user_session_path, :method => 'delete') %>
    </div>
  </div>
</div>
