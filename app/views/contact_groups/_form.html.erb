<%= form_with(model: contact_group, local: true) do |form| %>
  <% if contact_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact_group.errors.count, "error") %> prohibited this contact_group from being saved:</h2>

      <ul>
        <% contact_group.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :group_name %>
    <%= form.text_field :group_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
